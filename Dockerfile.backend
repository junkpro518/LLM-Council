FROM python:3.10-slim
WORKDIR /app

RUN pip install --no-cache-dir fastapi "uvicorn[standard]" httpx "pydantic>=2.9.0" python-dotenv

COPY backend/ ./backend/
COPY pyproject.toml ./

RUN mkdir -p data/conversations

CMD ["python", "-m", "backend.main"]
